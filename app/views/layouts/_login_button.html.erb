<% #Simply adding button login-logout in main page %>
<% if user_signed_in? %>
  <li> 
    Hi <strong><%= current_user.username %></strong>! 
    <%= link_to "Logout", destroy_user_session_path, method: :delete%>
  </li>
<% else %>
  <li> <%= link_to "Login", new_user_session_path%> </li>
<% end %>