<br> <br> <br>
<h1>Editing Admin Sponsor</h1>

<%= render 'form', sponsor: @sponsor, target_url: admin_event_sponsor_path %>

<%= link_to 'Show', admin_event_sponsor_path(@event, @sponsor) %> |
<%= link_to 'Back', admin_event_sponsors_path(@event) %>
